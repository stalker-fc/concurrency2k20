Компиляция и запуск реализованы с помощью Makefile.

Для компиляции необходимо выполнить:
>>> make build

Для запуска необходимо выполнить:
>>> make run

Сначала генирируются две матрицы A и B, потом происходит их перемножение. Размерность матриц определяется через константу N, описанную в Makefile.
