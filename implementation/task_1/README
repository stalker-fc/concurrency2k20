Исходники лабораторной работы находятся в папке ./sources
Компиляция и запуск реализованы с помощью Makefile.

Для компиляции необходимо выполнить:
>>> make build

Для запуска необходимо выполнить:
>>> make run

Сначала генерируются две матрицы A и B, потом происходит их перемножение. Размерность матриц определяется через константу N, описанную в Makefile.
